
import reactDom from "react-dom" ;

import "./index.css"

import App from "./App"

reactDom.render(<App></App>,document.getElementById("root"))  //At the time of passing the element we also write <App /> rather than writing the <App></App>